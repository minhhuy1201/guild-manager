# Tài khoản quản trị dùng cho form đăng nhập (chỉ đọc ở server, không expose ra client).
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

# Khóa bí mật dùng ký cookie phiên đăng nhập (HMAC-SHA256).
# Sinh khóa mới: openssl rand -hex 32
AUTH_SECRET=
